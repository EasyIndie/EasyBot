# EasyBot 环境变量
#
# 将此文件复制为 .env 并填入你的令牌/密钥。
# 设置令牌后对应平台的适配器会自动启用，无需修改 gateway.yaml。
#
# 此文件不受版本控制（.env 已在 .gitignore 中）。
# Shell export / Docker environment 优先于本文件的值。
#
# 独立运行:
#   easybot --init && vim ~/.easybot/.env && easybot --debug
#
# Docker Compose:
#   cp .env.example .env && vim .env && docker compose up -d
# Compose 默认只把 EasyBot/Prometheus 发布到宿主机 127.0.0.1。
#
# 取消 '#' 注释并填入令牌以启用对应平台。

# Telegram Bot Token（从 @BotFather 获取）
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Discord Bot Token
# DISCORD_BOT_TOKEN=your_discord_bot_token

# 飞书/Lark 应用凭据
# FEISHU_APP_ID=cli_xxxxxxxxxxxx
# FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx

# QQ 机器人凭据（AppId 作为 app_id，AppSecret 作为 token）
# QQ_APP_ID=your_qq_app_id
# QQ_CLIENT_SECRET=your_qq_client_secret

# 管理后台密码（生产环境至少 12 个字符）
# EASYBOT_ADMIN_PASSWORD=replace-with-a-long-random-password

# 生产模式会强制校验 TLS、密码、CORS、限流、Webhook 等安全设置
# EASYBOT_ENV=production

# 仅当可信反向代理已经终止 TLS 时启用；不要用于公网明文服务
# EASYBOT_ALLOW_PLAINTEXT=true

# Compose 端口绑定地址。商用环境保持 127.0.0.1，由宿主机 TLS 代理转发；
# 若代理也运行在 Compose 中，应直接访问 easybot:8080 并删除 ports 映射。
# EASYBOT_BIND_ADDRESS=127.0.0.1

# 个人微信：无需环境变量。启动后自动进入扫码登录，扫描屏幕二维码即可。

# PostgreSQL（可选，默认：SQLite）
# DATABASE_URL=postgresql://user:password@localhost:5432/easybot
